/**
 * Canonical CandidateGenerator re-exported from @excerpt/clipping-core.
 * Single Source of Truth for candidate boundary generation across the monorepo.
 */
export { CandidateGenerator } from '@excerpt/clipping-core';
