/**
 * Re-exporting canonical ranking profiles from @excerpt/clipping-core.
 * Single Source of Truth for ranking logic across the monorepo.
 */
export {
  RankingProfile,
  StandardizedRankingWeights,
  RANKING_PROFILES,
  getRankingWeightsForCategory,
} from '@excerpt/clipping-core';
